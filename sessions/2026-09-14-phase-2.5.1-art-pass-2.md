# Session — 2026-09-14 (later) — Phase 2.5.1 design sprint: save Clover's art pass 2 (tails + mane)

## Session setup
- Model / Effort / Uncertainty: Opus 5 · effort as set in the app, not re-confirmed · standard
- Git: same branch as the neck work, `claude/grazing-pose-neck-head-24efbd`. `origin/master` is still at `d75e679`, and PR #90 is open, so this goes on the same PR.
- Open holds at open: the long-neck holds from `sessions/2026-09-14-phase-2.5.1-graze-neck.md`.
- Hygiene note: that session log had already been written, so this save-and-check pass gets its own log (same shape as 2026-09-11). No code.

## What shipped
No code. Clover's export is committed as-is: **`prototypes/horse-parts-template-Edit-NewTail-Mane.svg`**, built on `Edit2.svg`.

What Clover changed:
- **Tails:** all four redrawn as flowing, strand-cut shapes. `tail-hang` is named correctly; the Illustrator wrapper group inside it is ignored.
- **Mane:** redrawn as long flowing strands that run back over the withers to x≈252. They're on the neck piece, so they move with it. The root capsule (bridge + r=46 disc) is kept.
- **Front bumper:** reshaped. Its top is at y≈104.5, and it still covers the barrel's top-left corner.
- **Head:** the top-back of the skull is trimmed slightly. The eye and nostril keep their names.

Pivots and the ground line are identical to `Edit2.svg`. Illustrator dropped `data-cap` again, which only hides the dashed cap circles in the harness.

## Verified in the harness
Checked with a scratch copy that loads the file as its built-in art; since deleted.
- 14 pieces, no warnings.
- **Graze:** withers −96.5°, poll +66.5°, muzzle 2u above the grass. All 4 hooves on the ground.
- **Walk:** 0u stance float; bob unchanged (0–11.5u).
- **Neck at both limits** ([−105, +80] and [+20, −45]): the mane strands stay attached and the silhouette stays in one piece.
- All four tails render.
- No console errors.
- **Clover's own check:** animation clean in the harness, no visible gaps in the posing.

## Decisions made
- **Keep Clover's filename; don't overwrite `Edit2.svg`.** *Claude.* It's the file open in her Illustrator. `Edit2.svg` stays as the generated base, and the generator can rebuild it byte-for-byte anyway.

## Uncertainty resolved
- **The ears survived the export.** The head path keeps its ear tips at y≈−10, above the artboard. Illustrator keeps artwork outside the artboard. Carried from the neck session.

## Testing holds
- From the neck session: the long neck reads right and the animation is clean. *Clover: "Looking pretty good", "no visible gaps".* The mane-teeth question is moot, since the mane was redrawn.
- Still open from 2026-09-10: puppet or in-style (partly answered by the above; confirm on the chip rows), static haunch, walk bob.

## Carryover
- **The working file is now `prototypes/horse-parts-template-Edit-NewTail-Mane.svg`.** Keep editing it in place.
- The uncommitted tail WIP in the main checkout's `Edit.svg` is superseded. Clover can discard it (`git restore prototypes/horse-parts-template-Edit.svg` in the main checkout) once she's sure nothing else is in there.
- Next Claude session: **pass 2 + posing/bake** (`tools/pose_horse.py`) · Opus · high, starting from this file.

## Deferred / added to roadmap
Nothing new.
