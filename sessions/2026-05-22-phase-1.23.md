# Session — 2026-05-22 — Phase 1.23 GitHub Actions Deploy

## Session setup
- Model / Effort / Uncertainty: Sonnet · medium · standard
- Git: master
- Open holds: none (1.14 + 1.15 + 1.19 fully cleared prior session)

---

## What happened

Also included in this session (continuation from prior context): two hotfixes shipped alongside phase close work:

**Sign-in double-click bug** — After Google OAuth, Clerk redirects to `/` rather than `/sign-in`, so `_verifyWithBackend()` never runs and the Flask session is never established. Fixed by adding a Clerk session bridge snippet to `base.html`: for any logged-out page other than `/sign-in`, loads Clerk and redirects to `/sign-in` if `Clerk.user` is truthy. Catches the OAuth redirect case without affecting normal flows.

**`/poetry` 500 for logged-out users** — `CURRENT_USER_DISPLAY_NAME` in `templates/poetry.html` used `null` (not a valid Jinja2 keyword) in the else-branch of a ternary. Logged-in users never hit the else-branch (short-circuit), so the bug was invisible during logged-in testing. Fixed by changing `null` → `none`.

**Phase 1.23 — GitHub Actions deploy:**

Spec written first (`spec/phase-1.23.md`), then workflow created at `.github/workflows/deploy.yml`. Two iteration passes:

1. First attempt: `uv` not found — non-interactive SSH sessions don't have `~/.local/bin` in PATH.
2. Second attempt: `source ~/.local/bin/env` — uncertain whether that file exists on this VPS.
3. Final: hardcoded full path `/home/metaphorever/.local/bin/uv` with `--python /home/metaphorever/.venv/bin/python` flag — no PATH or activation needed. Deployed and verified green.

One-time VPS setup done by Clover: generated ed25519 deploy key on the VPS, `cat ~/.ssh/poet-horse-deploy.pub >> ~/.ssh/authorized_keys` (ssh-copy-id loopback failed; used direct append instead), `loginctl enable-linger metaphorever` (returned nothing = success), private key added to GitHub Secrets as `DEPLOY_SSH_KEY`.

---

## Decisions made

- **Full uv path** `[confirmed]` — `/home/metaphorever/.local/bin/uv` with `--python` flag rather than activation or PATH manipulation. Robust to non-interactive SSH session environments.
- **Key generated on VPS, not locally** `[Clover, ok]` — spec assumed local generation + ssh-copy-id; Clover ran it on the VPS. Adapted by using direct `>> authorized_keys` append. Functionally identical outcome.

---

## Testing holds

- [x] Verified: deploy workflow runs green end-to-end (git pull, uv pip install, systemctl restart all succeed)

---

## Carryover

- None.
