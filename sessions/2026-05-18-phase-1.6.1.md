# Session: Phase 1.6.1 — Hotfix: restore submission flow end-to-end

```
Model:                Opus 4.7
Effort:               high
Uncertainty flagging: standard
Git:                  PR #23
Open holds:           1.6 manual verification (unblocked by this fix)
```

## What shipped

- **`init_db()` called at app startup** (`app.py`) — runs `db.conn.init_db()` on every boot, which applies `schema.sql` AND `db.seed.apply_migrations()` (idempotent ALTER TABLE for `inspired_by_text` / `inspired_by_url` / `users.trust_level`) and seeds the tag taxonomy. Root cause of submission 500: a production VPS DB init'd at Phase 0.2 was missing columns Phase 1.3 added; a fresh DB had no tables at all.
- **`/browse`** — minimal poem index (published poems, links, author, count, date). New template `templates/poem_index.html`. Stubs remain at `/featured` and `/random` per Phase 1.8 plans.
- **Mobile chip touch-drag fix** — `touch-action: none` and `-webkit-touch-callout: none` on `.horse-tile` so finger drags route to the `pointermove` handler rather than page scroll / iOS selection callout.

## Decisions

- `[confirmed]` Root cause: missing schema columns in production DB from pre-migration launch. Fix: idempotent migrations at startup, not a one-time script.
- `[confirmed]` `/browse` stub included in this PR rather than waiting for 1.8 — low-cost addition while the route was being touched.

## Testing holds cleared

- Submission flow verified end-to-end by Clover in production (2026-05-18).
- 1.6 view-mode toggle unblocked for manual verification.

## Note

*Catch-up log — written 2026-05-18 to reconcile docs with git. Session log was not written at close due to continued work after checklist. See new hard-stop rule in session close checklist.*
