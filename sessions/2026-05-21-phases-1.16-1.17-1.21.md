# Session — 2026-05-21 — ROADMAP sync + Phases 1.16, 1.17, 1.21

## Session setup
- Model / Effort / Uncertainty: Sonnet · medium · suppressed
- Open holds: 1.28 holds 1–9 cleared by Clover at session open; hold 10 (anon flow) deferred
- Overrides: spec-first waived for all three phases — well-defined from ROADMAP, no spec needed

---

## What happened

### ROADMAP reconciliation

At session open, discovered ROADMAP was significantly out of sync with the codebase — many phases built out of order and never marked shipped. Clover confirmed all of the following were already complete and working on the live site:

- **1.8** Featured / Browse / Random — built across earlier phases
- **1.9** Empty-line warning at publish — already verified
- **1.10** Export: copy as text / HTML / .txt — confirmed done
- **1.11** Plain-text print stylesheet — confirmed done
- **1.25** Nav / IA polish — confirmed done
- **1.26** Horse search area rework — confirmed done

Also removed duplicate 1.27/1.28 entries from the Remaining list (they were in both Shipped and Remaining). ROADMAP now reflects actual state.

### Phase 1.16 — RSS feed

- `GET /feed.xml` — RSS 2.0, 50 newest published poems, title/author/horse-count/permalink per item
- `rfc2822` Jinja filter (handles both Unix timestamp floats and datetime objects)
- `<link rel="alternate" type="application/rss+xml">` autodiscovery in `base.html`
- `templates/feed.xml` created

### Phase 1.17 — Rate limiting

- Added `flask-limiter>=3.0,<5` to `requirements.txt` (4.1.1 installed)
- `Limiter` initialized with `memory://` storage, no global default
- Per-route limits:
  - `POST /submit`, `POST /submit/poem` → 10/minute
  - `POST /poetry/search`, `/poetry/rhyme/terms`, `/poetry/rhyme/horses`, `/poetry/thesaurus/horses` → 60/minute
  - `POST /auth/clerk/verify` → 20/minute

### Phase 1.21 — Soft sign-in prompts (editor)

- "My pasture" button: always rendered; `disabled` + `.qp-btn-signin` + `title="Sign in to load your pasture"` when logged out
- "Edit Details" button: always rendered; `disabled` + `.btn-signin` + `title="Sign in to name and save your draft"` when logged out
- Draft strip: logged-out users see `"Sign in to save your work as a draft"` prompt (`.draft-strip-guest`) instead of nothing
- CSS: `.draft-strip-guest`, `.draft-strip-signin-link`, `.btn-sec:disabled`/`.btn-sec.btn-signin` added

### Deploy

- VPS: `uv` at `/home/metaphorever/.local/bin/uv`, venv at `/data/home/metaphorever/.venv`
- `git pull && /home/metaphorever/.local/bin/uv pip install -r requirements.txt` — installed 5 new packages
- Service restarted, confirmed healthy
- DEPLOYMENT.md note: uv path should be updated (spec/technical.md references `/home/metaphorever/.venv/bin/uv` which doesn't exist)

---

## Decisions made

- **`memory://` limiter storage** — fine for 2 gunicorn workers pre-beta; gives 2× effective limit but acceptable. Revisit if multi-process becomes meaningful.
- **60/min on search routes** — generous for interactive use, enough to prevent Datamuse API hammering from a script
- **Disabled-not-hidden for 1.21** — greyed-out buttons with tooltips; draft strip shows a link prompt. No inline sign-in modal (defer to auth redirect backlog item if needed).

---

## Testing holds

All cleared by Clover on live site (2026-05-21):
- `/feed.xml` loads as valid RSS ✅
- Feed autodiscovery link in page source ✅
- Logged-out editor: pasture + edit-details buttons visible/greyed, draft strip shows sign-in prompt ✅
- Logged-in editor: no regression ✅

---

## Carryover / deferred

- **DEPLOYMENT.md / technical.md** — uv path on VPS is `/home/metaphorever/.local/bin/uv`, not `/home/metaphorever/.venv/bin/uv`. Should update docs before 1.23 (GitHub Actions) to avoid confusion.
- **1.23 GitHub Actions** — skipped this session; needs SSH keypair set up on VPS + GitHub secrets before workflow can run. Short dedicated session when Clover has VPS terminal time.
- **1.28 hold 10** (anon localStorage flow) — still deferred.

---

## Next session

No phase locked — pick from remaining queue. Suggested options by effort:
- **Quick** (no spec): 1.23 GitHub Actions deploy `[sonnet · medium]` — needs VPS SSH key setup first
- **Medium**: 1.14 Report button + queue, 1.15 Poet profile `/u/<slug>`, 1.19 Save + Pasture collections
- **Heavy** (needs spec session): 1.12 Three-mode display `[opus · high]`, 1.13 Moderation queue `[sonnet · high]`
