# Python
__pycache__/
*.py[cod]
*.pyo
.env
venv/
.venv/

# Secrets & tokens — never commit these
tumblr_tokens.json
.env.local

# Runtime state — generated at runtime, not source files
drafts.json
pasture.json
submissions.json

# Horse data legacy alias
horses_compressed.json.gz

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Claude local settings (per-user)
.claude/settings.local.json

# SQLite database (poems, users, tags) — created at runtime
data/poet.db
data/poet.db-journal
data/poet.db-wal
data/poet.db-shm
